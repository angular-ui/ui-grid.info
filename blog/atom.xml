<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title><![CDATA[UI Grid]]></title>
  <subtitle><![CDATA[An AngularJS data grid]]></subtitle>
  <link href="/blog/atom.xml" rel="self"/>
  <link href="http://ui-grid.info/blog/"/>
  <updated>2015-03-10T02:18:42.425Z</updated>
  <id>http://ui-grid.info/blog/</id>
  
  <author>
    <name><![CDATA[UI Grid Team]]></name>
    <email><![CDATA[uigridteam@gmail.com]]></email>
  </author>
  
  <generator uri="http://zespia.tw/hexo/">Hexo</generator>
  
  <entry>
    <title><![CDATA[6 Ways to Take Control of How Your Grid Data is Displayed]]></title>
    <link href="http://ui-grid.info/blog/6_ways_to_take_control_of_how_your_grid_data_is_displayed/"/>
    <id>http://ui-grid.info/blog/6_ways_to_take_control_of_how_your_grid_data_is_displayed/</id>
    <published>2015-02-27T16:55:01.000Z</published>
    <updated>2015-03-10T02:18:34.000Z</updated>
    <content type="html"><![CDATA[<!-- 5 ways to display data the way you want -->
<p>Getting your data displayed just right can be a huge pain.</p>
<p>You might just want to format some numbers, or you might want to embed something complex like a chart or custom directive.</p>
<p>In this post outline six different methods you can use to get your data <strong>just the way you want it</strong>:</p>
<ul>
<li>Bindings</li>
<li>Cell Filters</li>
<li>Cell Templates<ul>
<li>Links</li>
<li>Buttons</li>
<li>Custom directives</li>
</ul>
</li>
</ul>
<h2 id="Bindings">Bindings</h2>
<p>UI Grid columns can be bound to any sort of Angular expression, as well as property names that would normally break an expression:</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> columnDefs = [</div><div class="line">  { field: <span class="string">'name'</span> },</div><div class="line">  { field: <span class="string">'addresses[0]'</span> },</div><div class="line"></div><div class="line">  <span class="comment">/*</span></div><div class="line">    Yes, you can use hyphens, plus signs, etc.</div><div class="line"></div><div class="line">    Normally something like {{ row.entity.first-name }} would</div><div class="line">    bomb but UI Grid pre-processes your bindings to make sure they work correctly.</div><div class="line">  */</div><div class="line">  { field: <span class="string">'first-name'</span> }, </div><div class="line"></div><div class="line">  <span class="comment">/* Function expressions work too */</span></div><div class="line">  { field: <span class="string">'getCurrency()'</span> },</div><div class="line">  { field: <span class="string">'transformValue(row.entity.myField)'</span> },</div><div class="line"></div><div class="line">  <span class="comment">/* Plain old functions are also an option */</span></div><div class="line">  { field: <span class="function"><span class="keyword">function</span> <span class="params">()</span> </span>{ <span class="keyword">return</span> <span class="keyword">this</span>.address.zip; } }</div><div class="line">];</div></pre></td></tr></table></figure>

<h2 id="Cell_Filters">Cell Filters</h2>
<p>Cell filters can transform the displayed value for a column while leaving the model intact. In this plunker the amount column contains floating point values, but we only want to display the integer part. A simple cellFilter that uses <code>.toFixed()</code> will alter the displayed value the way we want.</p>
<iframe src="http://embed.plnkr.co/BjLqXGiUI8nQFwvijduh/preview" width="100%" height="300" frameborder="0" allowfullscreen></iframe>

<h3 id="Passing_Arguments">Passing Arguments</h3>
<p>In the third column I am using two different fields. The column itself is bound to the same field as the second column, but on the filter I am passing the scope like so: <code>cellFilter: &#39;currencyFilter:this&#39;</code>.  Using the <code>this</code> argument on your filter will pass the scope.</p>
<p>Then I can lookup the currency symbol I want using the currency field from the row.</p>
<p>If you double-click a cell in the Currency column you’ll see that the raw value is still available to be edited. Cell filters only change your displayed value!</p>
<h2 id="Cell_Templates">Cell Templates</h2>
<p>Column definitions take a <code>cellTemplate</code> argument you can use to give your cells a custom template. You can specify it a few different ways, with a url (relative or absolute), an Angular template ID, a string/Angular element, or a promise.</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> columnDefs = [</div><div class="line">  { field: <span class="string">'name'</span>, cellTemplate: <span class="string">'name-template.html'</span> },</div><div class="line">  { field: <span class="string">'name'</span>, cellTemplate: <span class="string">'myTemplateId'</span> },</div><div class="line">  { field: <span class="string">'name'</span>, cellTemplate: $.get(<span class="string">'url-to-your-template.html'</span>) }</div><div class="line">];</div></pre></td></tr></table></figure>

<h3 id="Bindings_in_Cell_Templates">Bindings in Cell Templates</h3>
<p>There are a couple options for your binding your row’s data in your template. You can access the <code>row</code> object which is in the local scope; <code>row.entity</code> will contain the reference to your object, so if you want the “name” field, you can bind like so: .</p>
<p>If you’ve <em>already</em> defined your binding in your column definition you can use one of UI Grid’s placeholders: <code>{{ COL_FIELD }}</code>. UI Grid will automatically replace COL_FIELD with the appropriate binding. If you need to two-way bind to your row, like when you’re using the edit feature, you’ll need to use the <code>MODEL_COL_FIELD</code> placeholder.</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> columnDefs = [</div><div class="line">  {</div><div class="line">    field: <span class="string">'image_url'</span>,</div><div class="line">    cellTemplate: <span class="string">'&lt;div class="ui-grid-cell-contents"&gt;&lt;img src="{{ COL_FIELD }}" /&gt;&lt;/div&gt;'</span></div><div class="line">  }</div><div class="line">];</div></pre></td></tr></table></figure>

<h3 id="Links_and_Buttons">Links and Buttons</h3>
<p>Links are simple, just use regular old HTML. Wrapping your cell in an element using the class <code>.ui-grid-cell-contents</code> will apply the proper CSS settings like padding, overflow, etc., and make sure your cell fits in its space nicely.</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> columnDefs = [</div><div class="line">  {</div><div class="line">    field: <span class="string">'email'</span>,</div><div class="line">    cellTemplate: <span class="string">'&lt;div class="ui-grid-cell-contents"&gt;&lt;a href="mailto:{{ COL_FIELD }}"&gt;Send E-Mail&lt;/a&gt;&lt;/div&gt;'</span></div><div class="line">  }</div><div class="line">];</div></pre></td></tr></table></figure>

<p>In the plunker below I’m using a basic cellTemplate to bind both the first and last name fields into one column. On the second column I’m using a simple link like above (note: none of these emails are real).</p>
<p>On the third one, I’m using a template that’s referenced in index.html. If you check the bottom of that file you’ll see the template inside a <code>&lt;script type=&quot;text/ng-template&quot;&gt;</code> tag. Any tags of that type will put automatically put into the template cache (<code>$templateCache</code>) by Angular, with the id you specify.</p>
<p>Also in the third column template you can see I’m using a variable called <code>grid.appScope</code>. Anywhere in your templates, <code>grid.appScope</code> will be bound to the parent scope of your grid. I use it to bind my button to a function in my controller that will open a new window with a url to google maps with the address in it (note: most if not all of these address will not exist).</p>
<iframe src="http://embed.plnkr.co/ciExi4w6706sbO4esU0q/" width="100%" height="300" frameborder="0" allowfullscreen></iframe>

<h3 id="Tooltips">Tooltips</h3>
<p>Tooltips are a little bit different because they float over your content. By default the grid’s cells are set to hide any overflow, so if you just pop a tooltip in there it won’t show up and you’ll probably be very confused. If you just add some custom overflow CSS it should fix your problem.</p>
<p>Hover over the names in the plunker below:</p>
<iframe src="http://embed.plnkr.co/v7a1W5mFHHaG894IDLTK/" width="100%" height="300" frameborder="0" allowfullscreen></iframe>

<p>Tooltips cannot overflow beyond the constraints of the viewport, so they won’t show above the first row or below the bottom row, for example.</p>
<h3 id="Custom_Directives">Custom Directives</h3>
<p>You can put absolutely anything in your cell templates, just remember to use <code>.ui-grid-cell-contents</code> if you’re not applying your own custom cell CSS.</p>
<iframe src="http://embed.plnkr.co/irskqERheTOiFql82QFC/preview" width="100%" height="300" frameborder="0" allowfullscreen></iframe>

<p>Here I’ve used d3.js, nvd3.js, and angular-nvd3.js to create sparkline charts in my cells.</p>
<h2 id="Summin’_it_up">Summin’ it up</h2>
<p>You should be able to do the vast majority of what you want with your data using these six methods. If you have questions, suggestions, or just want to say hi, drop in to <a href="http://gitter.im/angular-ui/ng-grid" target="_blank" rel="external">our gitter channel</a>; we’d love to meet you!</p>
]]></content>
    <summary type="html">
    <![CDATA[<!-- 5 ways to display data the way you want -->
<p>Getting your data displayed just right can be a huge pain.</p>
<p>You might just want to]]>
    </summary>
    
      <category term="customize" scheme="http://ui-grid.info/blog/tags/customize/"/>
    
      <category term="display" scheme="http://ui-grid.info/blog/tags/display/"/>
    
  </entry>
  
</feed>
